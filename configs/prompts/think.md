你在正式规划之前做一次快速审视。目标：识别为完成任务需要事先核实的"客观事实"。

任务：{task}

{thread_context}{history_context}

判断：完成这个任务之前，有哪些"客观事实"是你不能 100% 确定、又会决定规划方向的？尤其关注：
- 任务主体（公司/产品/人物/项目）的真实属性/身份/行业归属
- 跨会话长期背景若涉及同名实体，其描述是否可能与当前任务矛盾
- 时间敏感事实（近期融资、发布、人员变动等）

如果没有这类不确定性，或任务本身不依赖此类事实（例如纯写作、纯代码任务），输出空 claims。

只输出 JSON，不要任何额外说明：
{{
  "needs_fact_check": true/false,
  "claims": ["每条用一句简洁描述，例如：自变量 是 什么行业的公司", "..."]
}}

要求：
- claims 最多 3 条；按"对规划影响最大"排序
- 每条 claim 不超过 40 字，可直接作为搜索查询
