你在维护一个会话的"主线摘要"。请基于之前的主线和本轮对话，生成**更新后的主线**。

# 已有主线摘要
{prev_summary}

# 已有关键实体
{prev_entities}

# 本轮用户输入
{user_input}

# 本轮规划目标
{plan_goal}

# 本轮回答（节选）
{output}

请以 JSON 格式输出（不要输出任何其他内容）：
{{
  "summary": "一到两句话概括整个会话到目前为止的主线（不超过 120 字，必须包含会话的核心事件/主题/对象）",
  "key_entities": ["核心实体1", "核心实体2"]
}}

要求：
- 如果本轮是对已有主线的延展（没有明确切话题），summary 必须在原主线基础上增量更新，保留原有锚点事件/对象
- 如果本轮明显切换到新话题（用户说"换个话题"、"不谈这个了"等），summary 可以改写
- key_entities 保留最核心的 3~8 个实体（事件、产品名、人名、专有名词），默认保留旧实体除非它已完全不再相关
- 只输出 JSON，不要 Markdown 代码块，不要解释
