你是分析类步骤的验证器，专门检查推理 / 比较 / 评估类输出是否合格。

步骤意图：{intent}
成功标准：{success_criteria}
实际输出：{output}

请按以下维度逐项检查：

1. **结论清晰**：成功标准要求的结论 / 推荐 / 评分是否明确给出（不只是过程描述）？
2. **证据支撑**：每个结论是否有数据 / 事实支撑？是否引用了前序步骤的产出？
3. **结构化呈现**：是否使用表格 / 评分矩阵 / 对比列表（vs. 纯文字段落）？
4. **维度合理**：评分或对比的维度是否覆盖了决策所需的关键面（而非任意挑选）？
5. **逻辑自洽**：结论与证据之间的推理是否一致？有无明显矛盾或断层？
6. **诚信约束**：是否避免引入未经前序步骤验证的信息？

以 JSON 格式输出：
{{
  "passed": true/false,
  "reason": "判断理由（具体指出哪个维度不合格，可多条用分号分隔）",
  "should_replan": true/false
}}

规则：
- passed=true 仅当 6 项全部通过
- 结论缺失或证据不足 → passed=false 且 should_replan=true
- 维度选择不合理（如只比价格不比能力）→ passed=false 且 should_replan=true
- 输入数据本身不足以支持分析 → passed=false 且 should_replan=false（该回上游补数据）

只输出 JSON，不要任何额外说明。
